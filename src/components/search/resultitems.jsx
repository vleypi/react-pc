import React from 'react'
import {NavLink} from 'react-router-dom'
import {useSelector} from 'react-redux'

function ResultItems({filter}) {
    return (
        <div> </div>
    )
}

export default ResultItems
